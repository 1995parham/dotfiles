[general]
pythonfile = ~/.config/offlineimap/config.py
# List of accounts to be synced, separated by a comma.
accounts = aut, secret

[Account secret]
# Identifier for the local repository; e.g. the maildir to be synced via IMAP.
localrepository = secret-local
# Identifier for the remote repository; i.e. the actual IMAP, usually non-local.
remoterepository = secret-remote
synclabels = no

[Repository secret-local]
# OfflineIMAP supports Maildir, GmailMaildir, and IMAP for local repositories.
type = Maildir
# Where should the mail be placed?
localfolders = ~/mail/secret

[Repository secret-remote]
createfolders = no
# Remote repos can be IMAP or Gmail, the latter being a preconfigured IMAP.
type = IMAP
remotehost = mail.cock.li
remoteuser = 1995parham@hitler.rocks
remotepasseval = get_from_gopass("mail/1995parham@hitler.rocks")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Account aut]
# Identifier for the local repository; e.g. the maildir to be synced via IMAP.
localrepository = aut-local
# Identifier for the remote repository; i.e. the actual IMAP, usually non-local.
remoterepository = aut-remote
synclabels = no

[Repository aut-local]
# OfflineIMAP supports Maildir, GmailMaildir, and IMAP for local repositories.
type = Maildir
# Where should the mail be placed?
localfolders = ~/mail/aut

[Repository aut-remote]
createfolders = no
# Remote repos can be IMAP or Gmail, the latter being a preconfigured IMAP.
type = IMAP
remotehost = webmail.aut.ac.ir
remoteuser = parham.alvani
remotepasseval = get_from_gopass("aut/mail/parham.alvani@aut.ac.ir")
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
