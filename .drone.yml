kind: pipeline
name: default

steps:
  - name: install
    image: ubuntu:18.04
    commands:
      - useradd -m -s /bin/bash parham
      - su -l parham
      - yes "Y" | ./start.sh env
      - yes "Y" | ./install.sh -y
      - >
        [ $(readlink $HOME/.zshrc) = $PWD/zsh/zshrc ]
